spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reviewscope?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: reviewuser
    password: Rw2026_1320
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    database-platform: org.hibernate.dialect.MySQL8Dialect

  # ★★★ TEXT 필드 크기 증가 (리포트 내용 길게 저장) ★★★
  sql:
    init:
      mode: never